# Caboco-Amigo — variáveis de ambiente
# Copie para .env e preencha os valores
# NUNCA commite o .env !

# LLM (obrigatório)
GEMINI_API_KEY=sua_chave_aqui

LLM_PROVIDER=google
LLM_MODEL=gemini-1.5-flash